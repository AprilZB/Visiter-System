# 园区访客系统 (Visitor System) - 软件说明书与部署指南

**拟制单位**：浙江脉通智造科技有限公司  
**文档版本**：V2.0.0 (生产发布版)  
**发布日期**：2026年07月  

---

## 1. 软件概述与系统凭据

本系统专为浙江脉通智造科技有限公司定制开发，旨在解决传统纸质登记身份无法核验、协同效率低下、商业机密缺乏线上强保密协议（NDA）管控存证等痛点。系统打通外部访客、内部员工（钉钉端）、物业保安及 PC 系统管理员四方协同。

### 1.1 初始管理员系统凭据
* **PC 管理后台地址**：`http://服务器IP:8097/admin`
* **管理员默认账号**：`admin`
* **管理员默认密码**：`Accupath@0723`

---

## 2. 软件核心功能模块

### 0. 全功能测试导航门户 Portal (路径 `/`)
* **访问入口**：`http://IP:8097/`
* **门岗正门固定静态申请二维码**：展示园区正门贴码与打印控件，包含四大功能模块（访客端、员工端、保安端、管理员端）的直操测试入口卡片。

### 2.1 外部访客端 (H5 - 端口 8097)
* **访问入口**：`http://IP:8097/visitor`
* **场景 A (邮箱专属邀请链接进入)**：点击邮件中的 `visitorToken` 专属链接直达身份补全页，支持拍照/上传身份证进行 OCR 识别，补全后阅读盖章版 PDF 保密协议并完成手写签名，直接生成通行二维码。
* **场景 B (现场盲扫二维码进入)**：挑选受访部门（带出一级/二级全路径全名，自动过滤屏蔽隐藏部门）、选择受访员工（强力过滤离职人员）、选择拟到访日期与到访时间段（如 `09:00 ~ 18:00 (全天段)`），提交进入审批流。
* **保密协议 (NDA) PDF 强拦截签署**：显示盖章版 PDF 协议阅览器与 HTML5 Canvas 手写签名画板，生成包含 IP、时间戳及 SHA-256 数字存证的加密凭证。

### 2.2 内部员工端 (纯Token免密处理/微应用 - 端口 8097/8096)
* **访问入口**：`http://IP:8097/host`
* **功能 A (批量发邮件预预约)**：员工在界面输入来访单位、拟到访时间段，点击【+ 添加来访人员】批量录入多名访客（姓名、手机号、电子邮箱），系统自动为各访客生成唯一 `visitorToken` 并发送规范 HTML 到访邀请函邮件。
* **功能 B (场景 B 免密快捷审批)**：受访员工收到钉钉通知中的 `approveToken` 唯一链接，无需登录，点击链接即看即批；支持将该链接直接转发给代理人处理。
* **功能 C (30分钟超时自动上报直属主管)**：当现场盲扫申请超过配置的阈值（默认 30 分钟）受访员工未处理，后台跑批任务自动根据 APHR 组织架构找到其直属主管 (`manager_name`)，向主管发送钉钉催办提醒与代办链接。


### 2.3 物业保安核验放行端 (H5 - 端口 8097)
* **访问入口**：`http://IP:8097/security`
* **正门申请码一键弹出**：右上角提供【正门申请码】弹窗，可向现场无预约访客展示。
* **强脱敏安全展示**：扫描访客动态二维码后，界面只展示倒数 5~8 位掩码脱敏身份证号（如：`3301021234****1234`），且绝无保密协议与敏感个人信息暴露。
* **一键核销放行**：保安核对物理证件一致后点击【确认放行】，动态通行码即刻作废销号。

### 2.4 PC 管理后台 Dashboard (Web - 端口 8097)
* **访问入口**：`http://IP:8097/admin`
* **防骚扰部门屏蔽配置**：一键开启/关闭部门屏蔽开关（被屏蔽的部门与员工无法在访客端被盲选）。
* **来访事由字典维护**：在线维护与新增来访事由字典选项。
* **OCR API 动态配置**：支持管理员在线查看与修改 PaddleOCR 服务器地址（如配置 `http://10.11.100.238:8081/ocr`）。
* **保密协议版本控制**：发布新版本保密协议（如 V1.1.0），新版本上线后自动使历史签署对新到访失效。
* **全量审计追溯**：查验全量访客记录明文解密与 SHA-256 数字存证哈希链。


---

## 3. 数据库与本地/生产配置说明

系统架构要求全量数据（包含访客申请明细、受访人员、防骚扰部门配置、来访事由字典、NDA保密协议与签名存证哈希）存储在 **MySQL 数据库** 中。

### 3.1 本地 Windows 开发环境一键启动 (MySQL 模式)

项目根目录下提供了脚本 [`start-dev.ps1`](file:///d:/DEV/Visitor-System/start-dev.ps1)，已配置默认连接本地/指定 MySQL 数据库：

```powershell
# 在 PowerShell 中默认启动 (默认连接 localhost:3306，密码 Accupath@0723)
.\start-dev.ps1

# 若本地 MySQL 密码不同，可以随时带参数指定：
.\start-dev.ps1 -mysqlHost "localhost" -mysqlUser "root" -mysqlPass "123456" -mysqlDb "visitor_system_db"
```

脚本启动时会自动创建 `visitor_system_db` 数据库并自动建表与加载初始化数据。

---

## 4. 软件运维与容器化部署


## 4. Linux 下 Docker 生产容器化部署指南

生产环境建议使用 Docker 与 `docker-compose` 统一部署在 `10.11.100.202` 服务器上。

### 4.1 配置文件说明
项目目录下包含完整的容器化部署配置文件：
1. `docker-compose.yml`：全局容器编排文件。
2. `visitor-backend/Dockerfile`：后端 Spring Boot (Java 21 JRE) 镜像构建。
3. `visitor-frontend/Dockerfile`：前端 Vue 3 + Nginx (端口 8097) 镜像构建。
4. `visitor-frontend/nginx.conf`：Nginx 8097 端口静态服务与 `/api/` 转发配置。

### 4.2 Linux 一键部署命令步骤

#### 第一步：克隆/上传项目代码至 Linux 服务器
```bash
cd /opt
git clone <repository_url> Visitor-System
cd Visitor-System
```

#### 第二步：打包后端 Jar 包 (或直接利用挂载)
```bash
cd visitor-backend
mvn clean package -DskipTests
cd ..
```

#### 第三步：使用 docker-compose 一键构建并启动服务
```bash
# 启动前后端所有容器
docker-compose up -d --build
```

#### 第四步：检查容器运行状态与日志
```bash
# 查看容器运行状态
docker-compose ps

# 查看后端日志
docker-compose logs -f visitor-backend

# 查看前端 Nginx 日志
docker-compose logs -f visitor-frontend
```

---

## 5. 维护与安全注意事项

1. **组织架构定时同步**：系统于每天凌晨 02:00 自动运行增量 ETL 任务，从 `10.11.100.202:3306` 主库拉取清洗人员关系，主库访问权限维持**严格只读**。
2. **钉钉 IP 白名单**：生产环境部署后，请在钉钉开发者后台中将服务器公网 IP 添加至“服务器出口 IP 白名单”。
